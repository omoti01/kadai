/*
 * - 入力値の素数判定 -
 */
#include <stdio.h>
int main()
{
  int i;
  int num;
  int flag = 0;
 
  printf("数：");
  scanf("%d", &num);
 
  /* 素数判定 */
  for( i=2;i<num;++i ) {
    if( num%i==0 ) {
      flag = 1;
      break;
    }
  }
   if( flag==0 )
    printf("%d は素数です。\n",number);
  else
    printf("%d は素数ではありません。\n",number);

  return 0;
}
